# Episodic Memory & Trace Replay — Task List

> Generated by: generate-tasks
> Plan: ./plan.md
> Spec: ./spec.md (v0.2)
> Date: 2026-04-24
> Feature flag: `episodicMemory.enabled` (default `false` until S8 baseline collected — D-7)
>
> **Progress:** ✅ Stage S1 complete (C-1 green, 24/24 tests pass). Stages S2–S8 pending.

Tasks are grouped by the 8 stages defined in [plan.md](./plan.md) §Implementation Strategy. Within each stage, `[P]` marks tasks safe to run in parallel.

Dependency notation: `T-XX → T-YY` = YY depends on XX.

---

## Stage S1 — Schema + Scorer (no external deps) ✅ COMPLETE

- **T-01** ~~Add repo dev deps: `ajv`, `ajv-formats`.~~ **Superseded**: repo convention is zero-dep; implemented `tests/helpers/mini-json-schema.js` (minimal Draft 2020-12 subset) instead. No `package.json` added.
- **T-02** ✅ Copied `contracts/episode.schema.json` → `.github/schemas/episode.schema.json` as the runtime canonical.
- **T-03** ✅ Created `.github/skills/trace-replay/` with `SKILL.md` + skeleton `capture.js`, `retrieve.js`, `format-context.js`, `gc.js`, `embed.js` (all throw "not yet implemented"); `score.js` fully implemented.
- **T-04** ✅ Implemented `score.js` `computeOutcomeScore` (pure function, formula I-1 frozen; weights `{0.4, 0.3, 0.2, 0.1}` exposed as `Object.freeze`).
- **T-05** ✅ Implemented `score.js` `recalcAfterRegression` (formula I-2, `REGRESSION_DECAY = 0.15`, floored at 0).
- **T-06** ✅ Wrote `tests/test-episode-schema.js` — 10 cases covering 5 positive fixtures + 5 negative cases (missing required, bad pattern, score > 1, additional property, wrong schemaVersion const).
- **T-07** ✅ Created `specs/009-episodic-memory-trace-replay/fixtures/` with `success.json`, `failed.json`, `aborted.json`, `with-regression.json`, `bad-tag.json`.
- **T-08** ✅ Wrote `tests/test-outcome-score.js` — 14 cases: all-zero, all-one, clamps low/high, single-component weights tests/review, weights-sum invariant, realistic example, regression decay normal + floor + ceiling-via-input, 3 input-validation negatives.

### ✅ Checkpoint C-1 — **GREEN (2026-04-24)**
Verified: `node tests/test-outcome-score.js && node tests/test-episode-schema.js` → 24/24 pass. Schema frozen until bump.

**Outstanding notes:**
- T-01 deviation: chose zero-dep mini validator over ajv to match repo convention. Spec-008 is unaffected (it does not mandate ajv).
- Pre-existing failure `tests/test-skill-pack.js` references `specs/003-cross-repo-improvement-ideas/` which doesn't exist in this workspace — unrelated to this change.

---

## Stage S2 — Embedding + Storage (blocked by C-1)

- **T-09** → C-1: Add runtime dep `onnxruntime-node`. Document CPU-only baseline.
- **T-10** [P] → C-1: Configure Git LFS for `.github/models/*.onnx`. Update `.gitattributes`.
- **T-11** → T-09,T-10: Download and LFS-commit `all-MiniLM-L6-v2.onnx` + `tokenizer.json`. Pin SHA256 in [research.md](./research.md) §R-1.
- **T-12** [P] → T-09: Implement `trace-replay/embed.js` — load model once, `embed(text): Float32Array(384)`. No batching in v1.
- **T-13** [P] → T-12: Seeded fake-embedding module `trace-replay/embed.fake.js` for tests (deterministic hash → stable vector). Used when `NODE_ENV=test` or `EMBEDDING_BACKEND=fake`.
- **T-14** → T-12: Write `tests/test-embed.js` — 3 cases: deterministic on repeat, different text → different vector, cosine(v,v)==1 ± ε.
- **T-15** → T-13: Implement index manifest I/O in `trace-replay/index-io.js` — read/write `manifest.json`, append vector to blob. Atomic write-then-rename (I-5).
- **T-16** → T-15: Implement `capture.js` full flow: read trace JSONL → compute summary, detected, score → redact via `redact-sensitive-data` skill → embed via `embed.js` → atomic write episode JSON + manifest entry.
- **T-17** → T-16: Write `tests/test-capture.js` — happy path + 3 failure modes: PII detected (skipped, parent run unaffected), duplicate `episode_id` (skipped), malformed trace (throws).

### ✅ Checkpoint C-2 (after S2)
Verify: capture produces schema-valid episode from a recorded trace fixture; manifest entry present; file atomically written.

---

## Stage S3 — Retrieval + Context Formatter (blocked by C-2)

- **T-18** → C-2: Implement `retrieve.js` — flat cosine sweep over manifest, threshold + score filter, negative-cap (D-5), model-drift detection (I-4), `replay_count` increment.
- **T-19** [P] → C-2: Implement `format-context.js` — XML-like fencing, byte-budget truncation, empty-in-empty-out, prompt-injection guard (I-3: never reference `raw_intent`).
- **T-20** [P] → C-2: Write `tests/test-retrieve.js` — 6 cases: happy top-3, threshold filter, model drift → cold-start + flag, negative classification, `!bad` tag override, empty store returns empty arrays (US-2 AC-6).
- **T-21** [P] → T-19: Write `tests/test-format-context.js` — 5 cases: positive-only, negative-only, mixed, byte-budget truncation preserves positives, empty → empty string.
- **T-22** → T-19: Write `tests/test-prompt-injection.js` — store episode with `raw_intent` containing "Ignore previous instructions"; assert formatter output does not contain that substring (quickstart Scenario 3).

### ✅ Checkpoint C-3 (after S3)
Verify: all retrieve/format tests green. Prompt-injection test MUST be part of CI regression set.

---

## Stage S4 — Skill + Autorun Wiring (blocked by C-3)

- **T-23** → C-3: Write full [.github/skills/trace-replay/SKILL.md](../../.github/skills/trace-replay/SKILL.md) content: description, when-to-use, inputs/outputs, 3 examples (capture, retrieve, A/B). Verify discoverability with `skill-discoverability-audit` skill.
- **T-24** → T-23: Patch [.github/prompts/autorun.prompt.md](../../.github/prompts/autorun.prompt.md) Phase 1: after `specify-feature`/spec-load, call `trace-replay.retrieve` when `config.episodicMemory.enabled === true`, inject block, emit `episode_replay` trace event.
- **T-25** [P] → T-23: Patch [.github/skills/autorun/SKILL.md](../../.github/skills/autorun/SKILL.md) Phase 1 to mirror T-24 (CLI parity).
- **T-26** → T-24: Patch Phase 7 in both surfaces: after `generate-evidence-summary`, call `trace-replay.capture`. Capture failure does **not** fail the parent run (non-blocking per `capture.js` contract).
- **T-27** → T-26: Extend [.github/schemas/trace.schema.json](../../.github/schemas/trace.schema.json) with `episode_replay` action in the EventRecord `action` enum-pattern (it already accepts `^[a-z][a-z0-9-]*$` so no change needed — verify and document).
- **T-28** → T-27: Integration test `tests/test-autorun-replay-integration.js` — synthesize a Phase-7 trace, run capture, start a mocked Phase-1 on a similar PBI, assert EPISODIC_MEMORY block appears in captured context dump.

### ✅ Checkpoint C-4 (after S4)
Verify: end-to-end integration test green. `episodicMemory.enabled=false` → no new behavior (NFR-7 degradation check).

---

## Stage S5 — CLI flags + config schema (blocked by C-3; runs in parallel with S4)

- **T-29** [P] → C-3: Patch [specs/008-prove-by-api-flow/contracts/autorun.config.schema.json](../008-prove-by-api-flow/contracts/autorun.config.schema.json) — add `episodicMemory` subtree matching [contracts/autorun-cli-flags.md](./contracts/autorun-cli-flags.md) config example. `additionalProperties:false` preserved.
- **T-30** [P] → C-3: Patch [specs/008-prove-by-api-flow/contracts/autorun-cli.md](../008-prove-by-api-flow/contracts/autorun-cli.md) — register `--no-replay`, `--replay-k`, `--replay-threshold` flags; register `insufficient-control-data` gate (category `config`, non-blocking).
- **T-31** → T-29: Update `.github/autorun.config.example.json` with the new subtree (`enabled: false` default).
- **T-32** → T-29,T-30: Implement CLI flag parser updates. Flags override config values for the invocation.
- **T-33** → T-32: Write `tests/test-cli-flags.js` — 4 cases: flag present overrides config, flag absent uses config, invalid k rejected, threshold out-of-range rejected.

### ✅ Checkpoint C-5 (after S5)
Verify: config schema still round-trips the example file; flags parse as specified.

---

## Stage S6 — GC + Negative-Example Review Guard (blocked by C-3)

- **T-34** → C-3: Implement `gc.js` per research.md R-5 — eviction rank formula, atomic delete, lazy vector-blob compaction.
- **T-35** → T-34: Write `tests/test-gc.js` — 5 cases: below-cap no-op, above-cap evicts lowest-rank, replay_count protects high-replay, age decay bounded at 0.5, dry-run returns plan without mutation.
- **T-36** [P] → C-3: Patch [.github/skills/review-spec/SKILL.md](../../.github/skills/review-spec/SKILL.md) — when plan lists a `NEGATIVE_EXAMPLE` episode in Phase 1 context, plan §Risks MUST cite the episode id (US-5 AC-2). Add as §Testability check item.
- **T-37** → T-36: Write `tests/test-review-spec-negative-guard.js` — 2 cases: plan cites id (pass), plan omits id (fail with actionable message).

### ✅ Checkpoint C-6 (after S6)

---

## Stage S7 — A/B surface (blocked by C-4, C-5)

- **T-38** → C-4,C-5: Patch [.github/skills/review-effectiveness/SKILL.md](../../.github/skills/review-effectiveness/SKILL.md) — add `--episodes` mode per [contracts/autorun-cli-flags.md](./contracts/autorun-cli-flags.md) gate section.
- **T-39** → T-38: Implement the partitioning logic (presence of `--no-replay` in MetaRecord).
- **T-40** → T-39: Implement Mann-Whitney U annotation for n≥30 per arm; below that, render medians only.
- **T-41** → T-38: Implement `insufficient-control-data` gate emission when `--no-replay` sample < 20.
- **T-42** → T-40,T-41: Write `tests/test-review-effectiveness-episodes.js` — 4 cases: insufficient data → warning gate, 20–29 samples → medians only, ≥30 samples → U-annotation, identical distributions → no-effect verdict.

### ✅ Checkpoint C-7 (after S7)

---

## Stage S8 — Baseline Collection (manual; blocked by C-7)

- **T-43** → C-7: Run 20 `/autorun --no-replay` invocations on this repo across varied PBIs. Record trace files.
- **T-44** → T-43: Run `review-effectiveness --episodes` — confirm report renders medians (no U-significance claims yet).
- **T-45** → T-44: If results look sane, flip `episodicMemory.enabled` default to `true` in `autorun.config.example.json`. If not, file as investigation.
- **T-46** → T-45: Update [spec.md](./spec.md) §2 baseline table with actual measured numbers; promote the `[Measured...]` placeholder to real values.

### ✅ Checkpoint C-8 (feature ready for promotion)

---

## Parallel Groups

| Group | Tasks | Notes |
|---|---|---|
| G-S1 | T-02, T-03 | Schema copy + skeleton |
| G-S1-tests | T-06, T-08 | Schema + scorer tests |
| G-S2 | T-10, T-12, T-13 | LFS config, embed, fake-embed |
| G-S3 | T-19, T-20, T-21 | Formatter + retrieve tests |
| G-S4-vs-S5 | Stage S4 and Stage S5 | Run in parallel after C-3 |
| G-S6 | T-34, T-36 | GC and review-spec guard |

---

## Requirements Coverage

| Req | Covered by |
|---|---|
| FR-1 Episode schema | T-02, T-06, T-07 |
| FR-2 `trace-replay` skill | T-03, T-23 |
| FR-3 Autorun Phase-1 extension | T-24, T-25 |
| FR-4 Outcome score | T-04, T-05, T-08 |
| FR-5 `review-effectiveness --episodes` | T-38, T-39, T-40 |
| FR-6 NEGATIVE_EXAMPLE + review-spec | T-19, T-22, T-36, T-37 |
| FR-7 Cross-repo export | **Deferred (D-3)** |
| FR-8 Score-gated GC | T-34, T-35 |
| FR-9 CLI flags | T-30, T-32, T-33 |
| FR-10 Local ONNX model | T-10, T-11, T-12 |
| FR-11 20-run baseline | T-41, T-43, T-44 |
| NFR-1 P95 < 500 ms | quickstart Scenario 4 (manual; no task) |
| NFR-5 No PII leaks | T-17, T-22 (injection + capture-redaction tests) |
| NFR-7 Graceful degradation | T-28 (flag-off path) |

---

## Execution Notes

- Every checkpoint requires tests **green** and schema validation clean before advancing.
- `[P]` tasks must not share files; verify via `git status` before parallel starts.
- Feature flag stays `false` from T-01 through T-44. Only T-45 flips it, and only after T-44 evidence looks sane.
- Total tasks: **46**. Estimated scope: mid-size feature; see spec-kit `estimate-effort` skill if team needs story-point rollup.

## Next Step

Re-enter `/autorun specs/009-episodic-memory-trace-replay` at Phase 3. With tasks defined, `@api-test-author` now has concrete red-test targets (T-06, T-08, T-14, T-17, T-20, T-21, T-22, T-28, T-33, T-35, T-37, T-42).
