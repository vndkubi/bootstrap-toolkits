'use strict';

/**
 * trace-replay/embed.js — Stage S2 skeleton.
 * Real backend: onnxruntime-node + all-MiniLM-L6-v2 (research.md §R-1).
 */

async function embed(_text) {
  throw new Error('embed.js not yet implemented (Stage S2; see tasks.md T-12)');
}

module.exports = { embed };
