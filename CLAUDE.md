@AGENTS.md

# Claude Notes

- Keep this file thin; shared project guidance lives in `AGENTS.md`.
- Use `.claude/settings.json` only for Claude-specific local permissions.
- When a workflow needs detailed Copilot bundle behavior, read the referenced `.github/docs/` or skill file on demand instead of loading the full `.github/` tree.
