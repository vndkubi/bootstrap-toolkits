[project]
name = "bootstrap-mcp"
version = "0.1.0"
description = "Portable MCP-style server for copilot-bootstrap read tools."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
bootstrap-mcp = "bootstrap_mcp.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
